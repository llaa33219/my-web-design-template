# 커스텀 디자인 시스템
> 디자인 시스템 문서

---

## 🎨 컬러 시스템

### Primary Colors (주 색상)
| 이름 | HEX | RGB | 용도 |
|------|-----|-----|------|
| Primary 50 | `#eef6ff` | rgb(238, 246, 255) | 매우 연한 배경, 호버 배경 |
| Primary 100 | `#b9d6fd` | rgb(185, 214, 253) | 연한 배경 |
| Primary 500 | `#007BFF` | rgb(0, 123, 255) | 기본 브랜드 색상, CTA 버튼 |
| Primary 600 | `#005BDD` | rgb(0, 91, 221) | 호버 상태 |
| Primary 700 | `#193694` | rgb(25, 54, 148) | 액티브 상태 |

### Secondary Colors (보조 색상)
| 이름 | HEX | RGB | 용도 |
|------|-----|-----|------|
| Secondary 50 | `#e3ffe3` | rgb(227, 255, 227) | 연한 배경 |
| Secondary 500 | `#00d200` | rgb(0, 210, 0) | 보조 액션, 성공 상태 |
| Secondary 600 | `#00a100` | rgb(0, 161, 0) | 호버 상태 |

### Neutral Colors (중립 색상)
| 이름 | HEX | RGB | 용도 |
|------|-----|-----|------|
| White | `#ffffff` | rgb(255, 255, 255) | 기본 배경 |
| Gray 50 | `#fafafa` | rgb(250, 250, 250) | 보조 배경 |
| Gray 100 | `#f5f5f5` | rgb(245, 245, 245) | 구분선, 비활성 배경 |
| Gray 200 | `#e8e8e8` | rgb(232, 232, 232) | 테두리 |
| Gray 300 | `#d6d6d6` | rgb(214, 214, 214) | 비활성 테두리 |
| Gray 400 | `#a6a6a6` | rgb(166, 166, 166) | 플레이스홀더 |
| Gray 500 | `#757575` | rgb(117, 117, 117) | 보조 텍스트 |
| Gray 600 | `#575757` | rgb(87, 87, 87) | 일반 텍스트 |
| Gray 700 | `#454545` | rgb(69, 69, 69) | 강조 텍스트 |
| Gray 800 | `#2b2b2b` | rgb(43, 43, 43) | 제목 |
| Gray 900 | `#1c1c1c` | rgb(28, 28, 28) | 최고 강조 |
| Black | `#000000` | rgb(0, 0, 0) | 순수 검정 (거의 사용 안 함) |

### Semantic Colors (의미 색상)
| 이름 | HEX | 용도 |
|------|-----|------|
| Success | `#00ef38` | 성공 메시지, 완료 상태 |
| Warning | `#ffdd00` | 경고, 주의 필요 |
| Error | `#df0013` | 에러, 삭제 액션 |
| Info | `#c9c9c9` | 정보 메시지 |

---

## 🔤 타이포그래피

### 폰트 패밀리
```css
/* 기본 폰트 (제목, 본문) */
--font-primary: "CloudSansCode", sans-serif;
/* 웹폰트 URL: https://cdn.jsdelivr.net/gh/projectnoonnu/2408@1.0/goorm-sans-code.woff2 */

/* 모노스페이스 (코드, 숫자) */
--font-mono: 'Google Sans Code', monospace;
/* Google Fonts: https://fonts.googleapis.com/css2?family=Google+Sans+Code */
```

### 폰트 스케일
| 이름 | 크기 | 굵기 | 행간 | CSS 변수 |
|------|------|------|------|----------|
| Display | 72px | 800 | 1.0 | `--text-display` |
| H1 | 56px | 700 | 1.1 | `--text-h1` |
| H2 | 36px | 700 | 1.2 | `--text-h2` |
| H3 | 28px | 600 | 1.3 | `--text-h3` |
| H4 | 24px | 600 | 1.3 | `--text-h4` |
| H5 | 20px | 600 | 1.4 | `--text-h5` |
| H6 | 18px | 600 | 1.4 | `--text-h6` |
| Body XL | 20px | 400 | 1.6 | `--text-body-xl` |
| Body Large | 30px | 400 | 1.6 | `--text-body-lg` |
| Body | 22px | 400 | 1.6 | `--text-body` |
| Body Small | 16px | 400 | 1.5 | `--text-body-sm` |
| Caption | 12px | 400 | 1.4 | `--text-caption` |

### 폰트 굵기
```css
--font-light: 300;
--font-regular: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
--font-extrabold: 800;
```

---

## 📐 간격 시스템 (Spacing Scale)

**8px 기반 시스템** - 모든 간격은 8의 배수 사용

| 이름 | 크기 | CSS 변수 | 용도 |
|------|------|----------|------|
| xs | 4px | `--space-xs` | 매우 작은 간격 (아이콘-텍스트) |
| sm | 8px | `--space-sm` | 작은 간격 (버튼 내부) |
| md | 16px | `--space-md` | 중간 간격 (요소 사이) |
| lg | 24px | `--space-lg` | 큰 간격 (그룹 사이) |
| xl | 32px | `--space-xl` | 매우 큰 간격 (섹션 내부) |
| 2xl | 48px | `--space-2xl` | 섹션 간격 |
| 3xl | 64px | `--space-3xl` | 큰 섹션 간격 |
| 4xl | 80px | `--space-4xl` | 매우 큰 섹션 간격 |
| 5xl | 96px | `--space-5xl` | 페이지 섹션 간격 |
| 6xl | 128px | `--space-6xl` | 최대 섹션 간격 |

---

## 🔲 레이아웃 시스템

### 컨테이너
```css
/* 최대 너비 */
--container-max-width: 1280px;

/* 좌우 패딩 */
--container-padding: 32px (var(--space-xl))

/* 중앙 정렬 */
margin: 0 auto;
```

### 그리드 시스템
```css
/* 12 컬럼 그리드 */
컬럼 수: 12
갭(Gap): 24px (var(--space-lg))
```

---

## 🧩 컴포넌트 스타일

### 1. 버튼

#### Primary Button
```css
배경: var(--primary-500) = #007BFF
텍스트: #ffffff
패딩: 10px 20px
테두리 반경: 20px (var(--radius-md))
폰트: 22px (var(--text-body)), 600 (Semibold)
그림자: 8px 8px 0px rgba(0,0,0,0.2) (var(--shadow-xs))

호버:
  배경: var(--primary-600) = #005BDD
  그림자: 2px 2px 0px rgba(0,0,0,0.2) (var(--shadow-sm))
  transform: translateY(6px)

액티브:
  배경: var(--primary-700) = #193694
  transform: translateY(8px)
  그림자: 0px 0px 0px rgba(0,0,0,0.2)

트랜지션: all 0.3s ease
```

#### Secondary Button
```css
배경: transparent
텍스트: var(--primary-500) = #007BFF
테두리: 2px solid var(--primary-500)
패딩: 10px 22px
테두리 반경: 20px (var(--radius-md))

호버:
  그림자: 8px 8px 0px rgba(0,0,0,0.2) (var(--shadow-xs))
  transform: translateY(-8px)

트랜지션: all 0.3s ease
```

#### Tertiary Button (Text Button)
```css
배경: transparent
텍스트: var(--primary-500) = #007BFF
패딩: 12px 16px
테두리: none

호버:
  배경: var(--gray-100) = #f5f5f5
  텍스트: var(--primary-600) = #005BDD

트랜지션: all 0.3s ease
```

#### 버튼 크기 변형
```css
Small:  패딩 8px 16px, 폰트 16px (var(--text-body-sm))
Medium: 패딩 10px 20px, 폰트 22px (var(--text-body)) - 기본
Large:  패딩 16px 32px, 폰트 30px (var(--text-body-lg))
```

---

### 2. 카드

#### 기본 카드
```css
배경: #ffffff
테두리: 1px solid var(--gray-200) = #e8e8e8
테두리 반경: 26px (var(--radius-lg))
패딩: 24px (var(--space-lg))
그림자: 2px 2px 0px rgba(0,0,0,0.2) (var(--shadow-sm))

호버:
  그림자: 10px 10px 0px rgba(0,0,0,0.2) (var(--shadow-lg))
  transform: translateY(-10px)

트랜지션: all 0.5s ease
```

#### 강조 카드
```css
배경: #ffffff
테두리: 5px solid var(--primary-500) = #007BFF
테두리 반경: 26px (var(--radius-lg))
패딩: 24px (var(--space-lg))
그림자: 2px 2px 0px rgba(0,0,0,0.2) (var(--shadow-sm))
```

---

### 3. 입력 필드 (Input)

#### Text Input
```css
배경: #ffffff
테두리: 1px solid var(--gray-300) = #d6d6d6
테두리 반경: 20px (var(--radius-md))
패딩: 12px 16px
폰트: 22px (var(--text-body)), 400 (Regular)
텍스트 색상: var(--gray-900) = #1c1c1c

플레이스홀더:
  색상: var(--gray-400) = #a6a6a6

포커스:
  아웃라인: none
  그림자: 8px 8px 0px rgba(0,0,0,0.2) (var(--shadow-xs))
  transform: translateY(-8px)

트랜지션: all 0.3s ease
```

#### Textarea
```css
/* Text Input과 동일하되 */
최소 높이: 120px
resize: vertical (세로 크기 조절 가능)
```

#### Select
```css
/* Text Input과 동일 */
```

---

### 4. 체크박스 & 라디오

#### Checkbox
```css
크기: 20px × 20px
accent-color: var(--primary-500) = #007BFF
cursor: pointer
```

#### Radio
```css
크기: 20px × 20px
accent-color: var(--primary-500) = #007BFF
cursor: pointer
```

---

### 5. 배지 (Badge)

#### 기본 배지
```css
패딩: 4px 12px
테두리 반경: 9999px (var(--radius-full), pill 형태)
폰트: 16px (var(--text-body-sm)), 500 (Medium)
```

#### 색상 변형
```css
Default: 배경 #a6a6a6 (var(--gray-400)), 텍스트 #ffffff
Primary: 배경 #007BFF (var(--primary-500)), 텍스트 #ffffff
Success: 배경 #29c54d, 텍스트 #ffffff
Warning: 배경 #f3c81e, 텍스트 #ffffff
Error:   배경 #e12e2e, 텍스트 #ffffff
```

---

### 6. 알림 (Alert)

```css
패딩: 16px (var(--space-md))
테두리 반경: 20px (var(--radius-md))
테두리: none
폰트: 22px (var(--text-body))
하단 여백: 24px (var(--space-lg))

색상 변형:
Info:    배경 #b9b9b9, 그림자 8px 8px 0px #767676, 텍스트 #ffffff
Success: 배경 #29c54d, 그림자 8px 8px 0px #006f1a, 텍스트 #ffffff
Warning: 배경 #f3c81e, 그림자 8px 8px 0px #dbac2c, 텍스트 #ffffff
Error:   배경 #e12e2e, 그림자 8px 8px 0px #901414, 텍스트 #ffffff
```

---

### 7. 네비게이션 바

```css
배경: #ffffff
높이: 72px
그림자: 8px 8px 0px rgba(0,0,0,0.2) (var(--shadow-xs))
위치: sticky top 0
z-index: 100

로고:
  폰트: 24px (var(--text-h4)), 700 (Bold)
  색상: var(--primary-500) = #007BFF

링크:
  패딩: 8px 16px
  폰트: 22px (var(--text-body)), 700 (Bold)
  색상: var(--gray-600) = #575757
  간격: 8px (var(--space-sm))

  호버:
    색상: var(--primary-500) = #007BFF

  액티브 (현재 페이지):
    색상: var(--primary-500) = #007BFF

트랜지션: all 0.15s ease
```

---

## 🎭 그림자 시스템 (Shadow Scale)

**특징: 하드 쉐도우 (Hard Shadow) 스타일**

```css
--shadow-xs:  8px 8px 0px rgba(0,0,0,0.2);
--shadow-sm:  2px 2px 0px rgba(0,0,0,0.2);
--shadow-md:  4px 4px 0px rgba(0,0,0,0.2);
--shadow-lg:  10px 10px 0px rgba(0,0,0,0.2);
--shadow-xl:  20px 20px 0px rgba(0,0,0,0.2);
--shadow-2xl: 25px 25px 0px rgba(0,0,0,0.2);
```

**디자인 특징**: 블러 없는 단단한 그림자로 **네오브루탈리즘(Neobrutalism)** 또는 **플랫 디자인** 스타일을 구현합니다.

---

## 🔄 테두리 반경 (Border Radius)

```css
--radius-sm:   14px;   /* 작은 요소 */
--radius-md:   20px;   /* 일반 요소 (버튼, 입력 필드) */
--radius-lg:   26px;   /* 큰 요소 (카드) */
--radius-xl:   30px;   /* 매우 큰 요소 */
--radius-full: 9999px; /* 완전한 원형 (배지, 프로필) */
```

**디자인 특징**: 상대적으로 큰 테두리 반경으로 **부드럽고 친근한 느낌**을 줍니다.

---

## ⚡ 애니메이션 & 트랜지션

### 트랜지션 타이밍
```css
--duration-fast:   0.15s;  /* 빠른 피드백 (네비게이션 링크) */
--duration-normal: 0.3s;   /* 일반 트랜지션 (버튼, 입력 필드) */
--duration-slow:   0.5s;   /* 느린 트랜지션 (카드) */

--easing: ease;
```

### 인터랙션 패턴

#### 버튼 Primary
- **호버**: 아래로 6px 이동 + 그림자 축소 (누르는 듯한 효과)
- **액티브**: 아래로 8px 이동 + 그림자 제거 (완전히 눌린 효과)

#### 버튼 Secondary
- **호버**: 위로 8px 이동 + 그림자 생성 (떠오르는 효과)

#### 카드
- **호버**: 위로 10px 이동 + 그림자 확대 (떠오르는 효과)

#### 입력 필드
- **포커스**: 위로 8px 이동 + 그림자 생성 (선택된 효과)

**디자인 특징**: **물리적 움직임**을 강조한 인터랙션으로 사용자에게 명확한 피드백을 제공합니다.

---

## 📱 반응형 브레이크포인트

```css
/* Mobile First Approach */
모바일: 0px ~ 639px
태블릿: 640px ~ 1023px
데스크톱: 1024px 이상
```

### 반응형 조정 사항

#### 모바일 (< 640px)
```css
/* 폰트 크기 조정 */
--text-h1: 36px (기본 56px에서 축소)
--text-h2: 28px (기본 36px에서 축소)
--text-h3: 24px (기본 28px에서 축소)

/* 컨테이너 패딩 */
padding: 0 16px (var(--space-md))

/* 섹션 패딩 */
padding: 64px 0 (var(--space-3xl))

/* 그리드 */
모든 그리드 → 1컬럼

/* 네비게이션 */
navbar-menu: display none (모바일 메뉴 숨김)
```

#### 태블릿 (640px ~ 1023px)
```css
/* 그리드 */
grid-3, grid-4 → 2컬럼
```

#### 데스크톱 (≥ 1024px)
```css
/* 기본 스타일 유지 */
```

---

## 🎨 디자인 스타일 특징

이 디자인 시스템은 다음과 같은 독특한 스타일을 가지고 있습니다:

### 1. 네오브루탈리즘 (Neobrutalism)
- **하드 쉐도우**: 블러 없는 단단한 그림자
- **강한 테두리**: 5px 두께의 강조 테두리
- **대담한 색상**: 높은 대비의 Primary 색상

### 2. 물리적 인터랙션
- **버튼 누름 효과**: 호버 시 아래로 이동하며 그림자 축소
- **카드 떠오름 효과**: 호버 시 위로 이동하며 그림자 확대
- **입력 필드 선택 효과**: 포커스 시 위로 이동

### 3. 부드러운 형태
- **큰 테두리 반경**: 20px ~ 30px의 둥근 모서리
- **친근한 느낌**: 부드러운 곡선으로 접근성 향상

### 4. 커스텀 폰트
- **CloudSansCode**: 기본 폰트로 독특한 브랜드 아이덴티티
- **Google Sans Code**: 코드 및 숫자 표시용

---

## 💡 사용 방법

### 1. 이 파일을 AI에게 제공
```
"첨부한 디자인 시스템을 따라 [페이지 설명]을 만들어줘"
```

### 2. 특정 컴포넌트만 사용
```
"디자인 시스템의 Primary Button과 Card 스타일을 사용해서 제품 카드를 만들어줘"
```

### 3. 색상만 변경
```
"디자인 시스템을 따르되, Primary 색상을 보라색(#7C3AED)으로 변경해줘"
```

---

## ✅ 이 디자인 시스템으로 할 수 있는 것

- ✅ 랜딩 페이지
- ✅ 대시보드
- ✅ 관리자 페이지
- ✅ 블로그
- ✅ 전자상거래 사이트
- ✅ 포트폴리오
- ✅ 문서 사이트
- ✅ 모든 종류의 웹 페이지

**특정 페이지 구조에 종속되지 않고, 재사용 가능한 디자인 요소만 정의**했기 때문에 어떤 페이지든 일관된 디자인으로 만들 수 있습니다.

---

## 🎯 주요 CSS 변수 요약

```css
:root {
    /* Colors */
    --primary-500: #007BFF;
    --secondary-500: #00d200;
    --gray-900: #1c1c1c;
    --white: #ffffff;
    
    /* Typography */
    --font-primary: "CloudSansCode", sans-serif;
    --text-body: 22px;
    --font-semibold: 600;
    
    /* Spacing */
    --space-md: 16px;
    --space-lg: 24px;
    --space-xl: 32px;
    
    /* Shadows (Hard Shadow) */
    --shadow-xs: 8px 8px 0px rgba(0,0,0,0.2);
    --shadow-sm: 2px 2px 0px rgba(0,0,0,0.2);
    
    /* Border Radius */
    --radius-md: 20px;
    --radius-lg: 26px;
    
    /* Transitions */
    --duration-normal: 0.3s;
    --easing: ease;
}
```

---

**생성일**: 2025-11-29  
**버전**: 1.0 (커스텀)  
**스타일**: 네오브루탈리즘 (Neobrutalism)  
**포맷**: Markdown
